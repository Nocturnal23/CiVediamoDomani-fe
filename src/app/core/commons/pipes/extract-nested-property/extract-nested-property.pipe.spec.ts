import { ExtractNestedPropertyPipe } from './extract-nested-property.pipe';

describe('Pipe: ExtractNestedPropertye', () => {
  it('create an instance', () => {
    const pipe = new ExtractNestedPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
